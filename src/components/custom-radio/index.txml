<view class="radio-wrapper" onTap="onTap">
  <view class="radio-item {{value === groupValue ? 'checked' : ''}}">
    <view tiki:if="{{value === groupValue}}" class="radio-dot" />
  </view>
  <text>{{text}}</text>
</view>
